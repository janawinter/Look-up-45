<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
  <script type="text/javascript" src="../client.js"></script>
  <script src="https://use.typekit.net/bkt2cmf.js"></script>
  <script>try{Typekit.load({ async: true });}catch(e){}</script>
  <link rel="stylesheet" href="./styles/main.css" charset="utf-8">
  <title>Look up 45</title>
</head>
<body>
  <div class="wrapper">
    <header>
      <div class="container">
        <h1>Look up 45</h1>
        <button class ="collection-button"><a href="/collection"> View My Collection</a></button>
      </div>
    </header>
    <div class="container">
      <section>
        <img src="/images/logo.png" alt="Look Up 45" />
        <div class="message"><strong></strong> has been added to collection</div>
      <section>
        <form action="/" method="post">
          <span>Enter your catalogue number:</span><input type="text" name="id"><button>Go</button>
        </form>
          </section>
          <section class= "results-table">
        {{#if releaseData}}
      <table>
        <thead>
          <th>Title</th>
          <th>Year</th>
          <th>Country</th>
          <th>Catalogue Number</th>
          <th>Format</th>
          <th>Price</th>
          <th>Add</th>
      </thead>
      <tbody>
     {{#each releaseData}}
      <tr>
        <td class="title">{{title}}</td>
        <td class="year">{{year}}</td>
        <td class="country">{{country}}</td>
        <td class="catno">{{catno}}</td>
        <td>
          {{#each format}}
            {{this}}
          {{/each}}
        </td>
        <td class="price">${{price}}</td>
        <td>
          <span class="release-id">{{id}}</span>
          <a href="#" class="add-to-collection">Add</a>
        </td>
      </tr>
      {{/each}}
    </tbody>
      </table>
      {{/if}}
    </section>
  </div>
 </div>
</body>
</html>
